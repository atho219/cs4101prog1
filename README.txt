Ross Thompson
Adrian Colon

As far as we know, everything works. The program creates a parser that returns
a node based on what is scanned in, building a tree. This tree is then
traversed and printed by the Node print function, which is overridden
depending on the type of Node and the number of parameters.